# Blackfynn Filter Project

## Instructions

### To run in development mode

1. `npm install`
1. `npm start`
1. Navigate to `http://localhost:8080` in browser

### To build and/or run project for deployments

1. `npm run build`
1. Deploy `dist` directory files and run `index.html` in browser
